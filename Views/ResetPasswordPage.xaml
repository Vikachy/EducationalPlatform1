<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="EducationalPlatform.Views.ResetPasswordPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Сброс пароля"
             BackgroundColor="{DynamicResource PageBackgroundColor}">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="30,40" VerticalOptions="Center">

            <!-- Заголовок -->
            <Label Text="Сброс пароля" 
                   FontSize="28"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="{DynamicResource PrimaryTextColor}"/>

            <!-- Информация о почте -->
            <Label x:Name="EmailMaskLabel"
                   Text="Код отправлен на: ***@example.com"
                   FontSize="14"
                   HorizontalOptions="Center"
                   TextColor="{DynamicResource SecondaryTextColor}"
                   Margin="0,0,0,10"/>

            <!-- Новый пароль -->
            <Frame BackgroundColor="{DynamicResource CardBackgroundColor}" 
                   Padding="15" 
                   HasShadow="True" 
                   CornerRadius="10">
                <VerticalStackLayout Spacing="12">
                    <Label Text="Новый пароль:" 
                           FontSize="16" 
                           FontAttributes="Bold"
                           TextColor="{DynamicResource PrimaryTextColor}"/>

                    <Grid ColumnDefinitions="*,Auto">
                        <Entry x:Name="NewPasswordEntry"
                               Grid.Column="0"
                               Placeholder="Введите новый пароль"
                               IsPassword="True"
                               FontSize="16"
                               BackgroundColor="Transparent"
                               TextColor="{DynamicResource PrimaryTextColor}"
                               PlaceholderColor="{DynamicResource SecondaryTextColor}"/>

                        <Button x:Name="TogglePassword1Button"
                                Grid.Column="1"
                                Text="👁"
                                BackgroundColor="Transparent"
                                TextColor="{DynamicResource PrimaryColor}"
                                Clicked="OnTogglePassword1Clicked"
                                Margin="5,0,0,0"/>
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Подтверждение пароля -->
            <Frame BackgroundColor="{DynamicResource CardBackgroundColor}" 
                   Padding="15" 
                   HasShadow="True" 
                   CornerRadius="10">
                <VerticalStackLayout Spacing="12">
                    <Label Text="Подтвердите пароль:" 
                           FontSize="16" 
                           FontAttributes="Bold"
                           TextColor="{DynamicResource PrimaryTextColor}"/>

                    <Grid ColumnDefinitions="*,Auto">
                        <Entry x:Name="ConfirmPasswordEntry"
                               Grid.Column="0"
                               Placeholder="Повторите новый пароль"
                               IsPassword="True"
                               FontSize="16"
                               BackgroundColor="Transparent"
                               TextColor="{DynamicResource PrimaryTextColor}"
                               PlaceholderColor="{DynamicResource SecondaryTextColor}"/>

                        <Button x:Name="TogglePassword2Button"
                                Grid.Column="1"
                                Text="👁"
                                BackgroundColor="Transparent"
                                TextColor="{DynamicResource PrimaryColor}"
                                Clicked="OnTogglePassword2Clicked"
                                Margin="5,0,0,0"/>
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Требования к паролю -->
            <Frame BackgroundColor="{DynamicResource InfoBackgroundColor}" 
                   Padding="12" 
                   CornerRadius="8"
                   BorderColor="{DynamicResource InfoBorderColor}">
                <VerticalStackLayout Spacing="6">
                    <Label Text="Требования к паролю:" 
                           FontSize="14" 
                           FontAttributes="Bold"
                           TextColor="{DynamicResource InfoTextColor}"/>
                    <Label Text="• Не менее 6 символов" 
                           FontSize="12"
                           TextColor="{DynamicResource InfoTextColor}"/>
                    <Label Text="• Пароли должны совпадать" 
                           FontSize="12"
                           TextColor="{DynamicResource InfoTextColor}"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Кнопка сброса пароля -->
            <Button x:Name="ResetButton"
                    Text="Сменить пароль"
                    Clicked="OnResetPasswordClicked"
                    BackgroundColor="{DynamicResource PrimaryColor}"
                    TextColor="White"
                    FontSize="16"
                    FontAttributes="Bold"
                    CornerRadius="10"
                    HeightRequest="50"
                    Margin="0,20,0,0"/>

            <!-- Кнопка отмены -->
            <Button Text="Отмена"
                    Clicked="OnCancelClicked"
                    BackgroundColor="Transparent"
                    TextColor="{DynamicResource SecondaryTextColor}"
                    FontSize="14"
                    BorderColor="{DynamicResource SecondaryTextColor}"
                    BorderWidth="1"
                    CornerRadius="10"
                    HeightRequest="40"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>