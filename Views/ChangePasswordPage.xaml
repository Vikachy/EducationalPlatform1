<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EducationalPlatform.Views.ChangePasswordPage"
             Title="Смена пароля">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20">

            <!-- Заголовок -->
            <Label Text="Смена пароля" 
                   FontSize="24" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center" />

            <!-- Текущий пароль -->
            <VerticalStackLayout Spacing="5">
                <Label Text="Текущий пароль" 
                       FontSize="14" 
                       FontAttributes="Bold" />
                <Grid ColumnDefinitions="*,Auto">
                    <Entry x:Name="CurrentPasswordEntry" 
                           Grid.Column="0"
                           Placeholder="Введите текущий пароль"
                           IsPassword="True" />
                    <Button x:Name="ToggleCurrentBtn"
                            Grid.Column="1"
                            Text="👁"
                            BackgroundColor="Transparent"
                            Clicked="OnToggleCurrentClicked" />
                </Grid>
            </VerticalStackLayout>

            <!-- Новый пароль -->
            <VerticalStackLayout Spacing="5">
                <Label Text="Новый пароль" 
                       FontSize="14" 
                       FontAttributes="Bold" />
                <Grid ColumnDefinitions="*,Auto">
                    <Entry x:Name="NewPassword1Entry" 
                           Grid.Column="0"
                           Placeholder="Введите новый пароль (мин. 6 символов)"
                           IsPassword="True" />
                    <Button x:Name="ToggleNew1Btn"
                            Grid.Column="1"
                            Text="👁"
                            BackgroundColor="Transparent"
                            Clicked="OnToggleNew1Clicked" />
                </Grid>
            </VerticalStackLayout>

            <!-- Подтверждение пароля -->
            <VerticalStackLayout Spacing="5">
                <Label Text="Подтверждение пароля" 
                       FontSize="14" 
                       FontAttributes="Bold" />
                <Grid ColumnDefinitions="*,Auto">
                    <Entry x:Name="NewPassword2Entry" 
                           Grid.Column="0"
                           Placeholder="Повторите новый пароль"
                           IsPassword="True" />
                    <Button x:Name="ToggleNew2Btn"
                            Grid.Column="1"
                            Text="👁"
                            BackgroundColor="Transparent"
                            Clicked="OnToggleNew2Clicked" />
                </Grid>
            </VerticalStackLayout>

            <!-- Кнопки -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <Button x:Name="ChangePasswordButton"
                        Grid.Column="0"
                        Text="Сменить пароль"
                        BackgroundColor="#2E86AB"
                        TextColor="White"
                        Clicked="OnChangePasswordClicked" />
                <Button x:Name="CancelButton"
                        Grid.Column="1"
                        Text="Отмена"
                        BackgroundColor="#6C757D"
                        TextColor="White"
                        Clicked="OnCancelClicked" />
            </Grid>

            <!-- Подсказка -->
            <Border BackgroundColor="#E3F2FD" 
                    Stroke="#2196F3" 
                    StrokeThickness="1" 
                    StrokeShape="RoundRectangle 10" 
                    Padding="15">
                <VerticalStackLayout Spacing="5">
                    <Label Text="Подсказка" 
                           FontSize="12" 
                           FontAttributes="Bold" 
                           TextColor="#1976D2" />
                    <Label Text="Пароль должен содержать не менее 6 символов" 
                           FontSize="11" 
                           TextColor="#1976D2" />
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>