<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EducationalPlatform.Views.CreateTheoryPage"
             Title="Создание теории">

    <Grid RowDefinitions="Auto,*">
        <!-- Современный заголовок -->
        <Border Grid.Row="0"
                BackgroundColor="{DynamicResource PrimaryColor}"
                Padding="20,15"
                StrokeThickness="0">
            <Label Text="Создание теоретического урока" 
                   FontSize="22"
                   FontAttributes="Bold"
                   TextColor="White"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
        </Border>

        <ScrollView Grid.Row="1" BackgroundColor="{DynamicResource BackgroundColor}">
            <VerticalStackLayout Spacing="20" Padding="20">

            <Border BackgroundColor="White"
                    Stroke="#E0E0E0"
                    StrokeThickness="1"
                    StrokeShape="RoundRectangle 12"
                    Padding="20">
                <VerticalStackLayout Spacing="18">

                    <Label Text="Название урока:"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource TextColor}"
                           Margin="0,0,0,5"/>
                    <Border BackgroundColor="#F8F9FA"
                            Stroke="#E0E0E0"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 8"
                            Padding="0">
                        <Entry x:Name="TitleEntry"
                               Placeholder="Введите название урока"
                               BackgroundColor="Transparent"
                               Margin="12,0"
                               FontSize="16"
                               HeightRequest="50" />
                    </Border>

                    <Label Text="Порядок урока:"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource TextColor}"
                           Margin="0,0,0,5"/>
                    <Border BackgroundColor="#F8F9FA"
                            Stroke="#E0E0E0"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 8"
                            Padding="0">
                        <Entry x:Name="OrderEntry"
                               Placeholder="1"
                               Keyboard="Numeric"
                               BackgroundColor="Transparent"
                               Margin="12,0"
                               FontSize="16"
                               HeightRequest="50" />
                    </Border>

                    <Label Text="Содержание:"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource TextColor}"
                           Margin="0,0,0,5"/>
                    <Border BackgroundColor="#F8F9FA"
                            Stroke="#E0E0E0"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 8"
                            Padding="0">
                        <Editor x:Name="ContentEditor"
                                HeightRequest="200"
                                Placeholder="Введите содержание урока простым текстом..."
                                AutoSize="TextChanges"
                                BackgroundColor="Transparent"
                                Margin="12,10"
                                FontSize="14" />
                    </Border>

                    <!-- Секция загрузки файлов -->
                    <Border BackgroundColor="#F0F7FF"
                            Stroke="{DynamicResource PrimaryColor}"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 10"
                            Padding="15">
                        <VerticalStackLayout Spacing="12">
                            <Label Text="Прикрепленные файлы:" 
                                   FontSize="16" 
                                   FontAttributes="Bold"
                                   TextColor="{DynamicResource PrimaryColor}" />

                            <Label Text="Разрешенные типы: ZIP, PDF, Word, PowerPoint, PNG" 
                                   FontSize="12" 
                                   TextColor="Gray" />

                            <Button Text="Выбрать файлы" 
                                    Clicked="OnSelectFilesClicked"
                                    BackgroundColor="{DynamicResource SecondaryColor}"
                                    TextColor="White"
                                    CornerRadius="8"
                                    HeightRequest="45"
                                    FontSize="16" />

                            <!-- Список выбранных файлов -->
                            <CollectionView x:Name="AttachmentsCollection" 
                                          HeightRequest="120">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnDefinitions="*,Auto,Auto" 
                                              Padding="10" 
                                              BackgroundColor="White"
                                              Margin="0,2">
                                            <Label Text="{Binding FileName}" 
                                                   FontSize="14"
                                                   VerticalOptions="Center"
                                                   LineBreakMode="TailTruncation" />
                                            <Label Grid.Column="1" 
                                                   Text="{Binding SizeFormatted}" 
                                                   FontSize="12"
                                                   TextColor="Gray"
                                                   VerticalOptions="Center"
                                                   Margin="10,0" />
                                            <!-- Значок корзины для удаления файла -->
                                            <Button Grid.Column="2" 
                                                    Text="🗑️"
                                                    Padding="5" 
                                                    Clicked="OnRemoveAttachmentClicked" 
                                                    CommandParameter="{Binding .}"
                                                    BackgroundColor="Transparent"
                                                    TextColor="Red"
                                                    FontSize="16"
                                                    WidthRequest="40"
                                                    HeightRequest="40" />
                                        </Grid>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>

                            <!-- Кнопка очистки будет управляться из кода -->
                            <Button x:Name="ClearAllButton"
                                    Text="Очистить все" 
                                    Clicked="OnClearAllFilesClicked"
                                    BackgroundColor="#FF6B6B"
                                    TextColor="White"
                                    IsVisible="False" />

                            <ProgressBar x:Name="UploadProgressBar" 
                                       Progress="0"
                                       IsVisible="False" />
                            <Label x:Name="UploadStatusLabel" 
                                   Text="" 
                                   FontSize="12"
                                   IsVisible="False" />
                        </VerticalStackLayout>
                    </Border>

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,10,0,0">
                        <Button Text="Назад"
                                Clicked="OnBackClicked"
                                BackgroundColor="#6C757D"
                                TextColor="White"
                                CornerRadius="8"
                                HeightRequest="50"
                                FontSize="16" />
                        <Button Text="Создать теорию"
                                Clicked="OnCreateClicked"
                                BackgroundColor="{DynamicResource PrimaryColor}"
                                TextColor="White"
                                CornerRadius="8"
                                HeightRequest="50"
                                FontSize="16"
                                FontAttributes="Bold" />
                    </Grid>

                </VerticalStackLayout>
            </Border>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>