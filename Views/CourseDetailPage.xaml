<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EducationalPlatform.Views.CourseDetailPage"
             Title="Курс">

    <Grid RowDefinitions="Auto,*">
        <Border Grid.Row="0" BackgroundColor="{DynamicResource PrimaryColor}" Padding="15">
            <HorizontalStackLayout>
                <Button Text="←" Clicked="OnBackClicked" BackgroundColor="Transparent" TextColor="White" />
                <Label x:Name="CourseTitle" Text="Курс" FontSize="20" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" />
            </HorizontalStackLayout>
        </Border>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="15" Spacing="12">
                <Button Text="Чат с преподавателем" x:Name="TeacherChatButton" Clicked="OnTeacherChatClicked" />
                <Label Text="📖 Теория" FontAttributes="Bold" />
                <CollectionView x:Name="TheoryCollection" SelectionMode="None" />

                <Label Text="🧩 Практика" FontAttributes="Bold" />
                <CollectionView x:Name="PracticeCollection" SelectionMode="Single" SelectionChanged="OnPracticeSelected" />

                <Label Text="📝 Тесты" FontAttributes="Bold" />
                <CollectionView x:Name="TestsCollection" SelectionMode="Single" SelectionChanged="OnTestSelected" />
            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>


