<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EducationalPlatform.Views.TeacherGroupsManagementPage"
             Title="Управление группами">

    <Grid RowDefinitions="Auto,*,Auto" BackgroundColor="{DynamicResource BackgroundColor}">
        <!-- Современный заголовок -->
        <Border Grid.Row="0" 
                BackgroundColor="{DynamicResource PrimaryColor}" 
                Padding="20,15"
                StrokeThickness="0">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <Label Grid.Column="1"
                       Text="Управление учебными группами" 
                       FontSize="22" 
                       FontAttributes="Bold" 
                       TextColor="White" 
                       HorizontalOptions="Center"
                       VerticalOptions="Center" />
            </Grid>
        </Border>

        <!-- Контент -->
        <ScrollView Grid.Row="1" BackgroundColor="{DynamicResource BackgroundColor}">
            <VerticalStackLayout Spacing="20" Padding="20">

                <!-- Создание новой группы -->
                <Border BackgroundColor="White" 
                        Stroke="{DynamicResource PrimaryColor}" 
                        StrokeThickness="1" 
                        StrokeShape="RoundRectangle 12" 
                        Padding="20">
                    <VerticalStackLayout Spacing="15">
                        <Label Text="Создать новую группу" 
                               FontSize="18" 
                               FontAttributes="Bold"
                               TextColor="{DynamicResource PrimaryColor}" />

                        <Border BackgroundColor="#F8F9FA"
                                Stroke="#E0E0E0"
                                StrokeThickness="1"
                                StrokeShape="RoundRectangle 8"
                                Padding="0">
                            <Entry x:Name="NewGroupNameEntry" 
                                   Placeholder="Название группы"
                                   BackgroundColor="Transparent"
                                   Margin="12,0"
                                   FontSize="16"
                                   HeightRequest="50" />
                        </Border>

                        <Border BackgroundColor="#F8F9FA"
                                Stroke="#E0E0E0"
                                StrokeThickness="1"
                                StrokeShape="RoundRectangle 8"
                                Padding="0">
                            <Picker x:Name="CoursePicker"
                                    Title="Выберите курс"
                                    ItemDisplayBinding="{Binding CourseName}"
                                    BackgroundColor="Transparent"
                                    Margin="12,0"
                                    FontSize="16"
                                    HeightRequest="50" />
                        </Border>

                        <Button Text="Создать группу" 
                                Clicked="OnCreateGroupClicked" 
                                BackgroundColor="{DynamicResource PrimaryColor}" 
                                TextColor="White"
                                CornerRadius="8"
                                HeightRequest="50"
                                FontSize="16"
                                FontAttributes="Bold" />
                    </VerticalStackLayout>
                </Border>

                <!-- Список групп -->
                <Label Text="Мои группы" 
                       FontSize="20" 
                       FontAttributes="Bold"
                       TextColor="{DynamicResource TextColor}"
                       Margin="0,10,0,10" />

                <CollectionView x:Name="GroupsCollectionView" 
                                ItemsSource="{Binding Groups}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,8" 
                                    BackgroundColor="White" 
                                    Stroke="{DynamicResource PrimaryColor}" 
                                    StrokeThickness="1" 
                                    StrokeShape="RoundRectangle 12" 
                                    Padding="16">
                                <Grid ColumnDefinitions="Auto,*,Auto" 
                                      RowDefinitions="Auto,Auto"
                                      VerticalOptions="Center">

                                    <!-- Иконка группы -->
                                    <Border Grid.Row="0" Grid.Column="0" 
                                            Grid.RowSpan="2"
                                            BackgroundColor="{DynamicResource PrimaryColor}"
                                            StrokeThickness="0"
                                            StrokeShape="RoundRectangle 20"
                                            WidthRequest="40"
                                            HeightRequest="40"
                                            VerticalOptions="Center"
                                            Margin="0,0,15,0">
                                        <Label Text="👥" 
                                               FontSize="20" 
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center" />
                                    </Border>

                                    <!-- Информация о группе -->
                                    <VerticalStackLayout Grid.Row="0" Grid.Column="1" Spacing="4">
                                        <Label Text="{Binding GroupName}" 
                                               FontSize="16" 
                                               FontAttributes="Bold"
                                               TextColor="{DynamicResource TextColor}" />
                                        <Label Text="{Binding CourseName}" 
                                               FontSize="13" 
                                               TextColor="#666" />
                                    </VerticalStackLayout>

                                    <Label Grid.Row="1" Grid.Column="1"
                                           Text="{Binding StudentCount, StringFormat='Студентов: {0}'}" 
                                           FontSize="12" 
                                           TextColor="#999"
                                           Margin="0,5,0,0" />

                                    <!-- Кнопки управления -->
                                    <Grid Grid.Row="0" Grid.Column="2"
                                          Grid.RowSpan="2"
                                          ColumnDefinitions="Auto,Auto"
                                          RowDefinitions="Auto,Auto"
                                          RowSpacing="8"
                                          ColumnSpacing="8"
                                          VerticalOptions="Center">

                                        <Button Grid.Row="0" Grid.Column="0"
                                                Text="Редактировать" 
                                                BackgroundColor="#FFC107" 
                                                TextColor="White" 
                                                FontSize="11"
                                                Padding="8,4"
                                                CornerRadius="6"
                                                Clicked="OnEditGroupClicked"
                                                CommandParameter="{Binding .}" />

                                        <Button Grid.Row="0" Grid.Column="1"
                                                Text="Студенты" 
                                                BackgroundColor="#4CAF50" 
                                                TextColor="White" 
                                                FontSize="11"
                                                Padding="8,4"
                                                CornerRadius="6"
                                                Clicked="OnManageStudentsClicked"
                                                CommandParameter="{Binding .}" />

                                        <Button Grid.Row="1" Grid.Column="0"
                                                Text="Чат" 
                                                BackgroundColor="#2196F3" 
                                                TextColor="White" 
                                                FontSize="11"
                                                Padding="8,4"
                                                CornerRadius="6"
                                                Clicked="OnChatClicked"
                                                CommandParameter="{Binding .}" />

                                        <Button Grid.Row="1" Grid.Column="1"
                                                Text="Удалить" 
                                                BackgroundColor="#F44336" 
                                                TextColor="White" 
                                                FontSize="11"
                                                Padding="8,4"
                                                CornerRadius="6"
                                                Clicked="OnDeleteGroupClicked"
                                                CommandParameter="{Binding .}" />
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Сообщение если групп нет -->
                <Border x:Name="NoGroupsLabel"
                        BackgroundColor="#FFF3E0"
                        Stroke="#FF9800"
                        StrokeThickness="1"
                        StrokeShape="RoundRectangle 10"
                        Padding="20"
                        IsVisible="False">
                    <Label Text="У вас пока нет учебных групп. Создайте первую группу!"
                           FontSize="15"
                           TextColor="#E65100"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center" />
                </Border>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Кнопка назад -->
        <Border Grid.Row="2"
                BackgroundColor="White"
                StrokeThickness="0"
                Padding="20,15">
            <Button Text="← Назад" 
                    Clicked="OnBackClicked" 
                    BackgroundColor="{DynamicResource SecondaryColor}" 
                    TextColor="White" 
                    CornerRadius="8"
                    HeightRequest="50"
                    FontSize="16"
                    FontAttributes="Bold" />
        </Border>
    </Grid>
</ContentPage>